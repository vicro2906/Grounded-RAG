"""Track B (Phase 4): LightRAG knowledge-graph retrieval for multi-hop questions.

Adds the entity-relation graph layer on top of the shared primitives in `rag.py` and maps its
selected chunks back to our payloads so citations keep working.
"""
